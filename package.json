{
  "name": "luma",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:rules": "firebase emulators:exec --only firestore --project demo-luma-rules \"node --test tests/firestore-rules.test.js\"",
    "deploy:hosting": "npm run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "@preact/compat": "^18.3.2",
    "firebase": "^12.17.0",
    "preact": "^10.29.7",
    "react-router-dom": "^6.30.2"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "@preact/preset-vite": "^2.10.6",
    "firebase-tools": "^15.26.0",
    "vite": "^8.2.0"
  }
}
